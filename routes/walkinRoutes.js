/**
 * Walk-In API Routes (Ivan)
 *
 * TODO (Ivan):
 * 1. Import express.Router(), techMiddleware
 * 2. Import walkinController
 * 3. Define routes (all require techMiddleware):
 *    GET    /api/walkin          -> walkinController.getAll       (get active walk-in reservations)
 *    POST   /api/walkin          -> walkinController.create       (create walk-in reservation)
 *    PUT    /api/walkin/:id/remove -> walkinController.removeNoShow (cancel/remove a no-show)
 * 4. Export router
 */

// TODO: Implement the above
