/**
 * Lab API Routes (Ivan)
 *
 * TODO (Ivan):
 * 1. Import express.Router()
 * 2. Import labController
 * 3. Define routes:
 *    GET /api/labs                -> labController.getAll        (returns all labs with availability counts)
 *    GET /api/labs/:code          -> labController.getByCode     (returns lab detail)
 *    GET /api/labs/:code/seats    -> labController.getSeats      (returns seat map with occupied/reserved/available status)
 *       Query params: ?date=YYYY-MM-DD&timeSlot=HH:MM
 * 4. Export router
 */

// TODO: Implement the above
