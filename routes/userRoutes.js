/**
 * User API Routes (Ivan)
 *
 * TODO (Ivan):
 * 1. Import express.Router(), authMiddleware
 * 2. Import userController
 * 3. Define routes (all require authMiddleware):
 *    GET    /api/users                -> userController.search      (query: ?q=searchTerm&college=CCS)
 *    GET    /api/users/:id            -> userController.getById
 *    PUT    /api/profile              -> userController.updateProfile
 *    PUT    /api/profile/password     -> userController.changePassword
 *    DELETE /api/profile              -> userController.deleteAccount
 * 4. Export router
 */

// TODO: Implement the above
