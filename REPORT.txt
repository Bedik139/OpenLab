================================================================================
OPENLAB COMPUTER RESERVATION SYSTEM - PROJECT REPORT
================================================================================

PROJECT OVERVIEW
----------------
A full-stack lab reservation system for DLSU built with Node.js, Express,
Handlebars, Mongoose (MongoDB), and Bootstrap. The project follows the
mc3-debttracker folder structure: index.js at the root as the entry point,
all source code inside src/ (models, routes, controllers, middleware,
helpers, views), and static assets in public/.


================================================================================
1. DEFAULT ACCOUNTS
================================================================================

STUDENT ACCOUNT
  Email:    maria_santos@dlsu.edu.ph
  Password: password123

TECHNICIAN ACCOUNT
  Email:    tech_admin@dlsu.edu.ph
  Password: admin123

OTHER SAMPLE STUDENT ACCOUNTS (all use password: password123)
  - jose_rizal@dlsu.edu.ph
  - ana_lopez@dlsu.edu.ph
  - karl_mendoza@dlsu.edu.ph
  - lea_cruz@dlsu.edu.ph


================================================================================
2. FILE STRUCTURE
================================================================================

Following the mc3-debttracker folder convention:
  - "main" file (index.js) at the root as the entry point
  - src/ contains all server-side source code
  - src/models/ ("database" folder) holds Mongoose schemas + conn.js
  - src/routes/mainRouter.js is one single router (for novice developers)
  - src/views/ stores Handlebars templates (front-end blueprints)
  - public/ is the static folder for CSS, JS, and uploaded assets

index.js                    Express app entry point (port 3000)
package.json                Dependencies and scripts
seed.js                     Database seed script
.env                        MONGODB_URI, SESSION_SECRET, PORT

public/                     Static folder (served by express.static)
  css/style.css             All styling
  js/app.js                 Frontend JS (fetch-based, no localStorage)
  assets/bg.png             Background image

src/
  models/                   Mongoose schemas (the "database/models" folder)
    conn.js                 MongoDB connection module (connect/disconnect)
    User.js                 User schema (bcrypt pre-save hook)
    Reservation.js          Reservation schema (compound index)
    Lab.js                  Lab schema
  routes/
    mainRouter.js           Single router — all page + API routes
  controllers/
    authController.js       Login, register, logout logic
    labController.js        Lab data and seat availability
    reservationController.js  Reservation CRUD
    userController.js       User search, profile, password
    walkinController.js     Walk-in reservation (technician only)
  middleware/
    auth.js                 authMiddleware + techMiddleware
  helpers/
    hbs-helpers.js          Handlebars helpers (eq, formatDate, etc.)
  views/                    Handlebars templates
    layouts/
      auth.hbs              Layout for login/register pages
      dashboard.hbs         Layout for authenticated pages
    partials/
      head.hbs              HTML <head> partial
      navbar.hbs            Navigation bar partial
    index.hbs               Landing page
    login.hbs               Login form
    register.hbs            Registration form
    adminsignup.hbs         Technician login
    dashboard.hbs           User dashboard
    cmpslots.hbs            Lab availability
    reserve.hbs             Seat selection and reservation
    reservations.hbs        View/manage reservations
    profile.hbs             User profile
    public-profile.hbs      Other user's public profile
    users.hbs               User search and directory
    walkin.hbs              Walk-in reservation (technician only)
    changepassword.hbs      Change password form
    error.hbs               Generic error page

specs/                      Project specification PDFs


================================================================================
3. HOW AUTHENTICATION WORKS
================================================================================

- User accounts are stored in MongoDB (users collection)
- Passwords are hashed with bcrypt (pre-save hook on User model)
- Login validates email + password via bcrypt.compare()
- On success, user object (without password) is stored in req.session.user
- Sessions are persisted in MongoDB via connect-mongo
- Registration creates a new User document (password auto-hashed)
- Duplicate emails and student IDs are rejected (unique index)
- After registration, the user is auto-logged in and redirected to dashboard
- Logging out destroys the session and clears the cookie
- Password changes use comparePassword() + pre-save hook for new hash


================================================================================
4. ROLE-BASED ACCESS
================================================================================

STUDENT can:
  - Register and login
  - View lab availability and seat maps
  - Reserve a seat (30-min slots)
  - View, edit, and cancel their own reservations
  - View and edit their profile
  - Change their password
  - Search for other users
  - Delete their account

TECHNICIAN can do everything a student can, PLUS:
  - See ALL reservations from all users (on reservations page)
  - See all-user stats on their dashboard
  - Access the Walk-In Reservation page (walkin.html)
  - Create walk-in reservations for students
  - Remove no-show reservations
  - Walk-In link appears in their navbar

PROTECTED PAGES (redirect to login if not authenticated):
  - dashboard.html
  - reservations.html
  - reserve.html
  - profile.html

TECHNICIAN-ONLY PAGE (students get "Access denied"):
  - walkin.html


================================================================================
5. DATA STORAGE (MongoDB + Sessions)
================================================================================

MongoDB Collections:
  users                   All registered accounts (passwords hashed with bcrypt)
  reservations            All reservations from all users
  labs                    Computer lab definitions (5 labs)
  sessions               Express session store (managed by connect-mongo)

Session (express-session + connect-mongo):
  req.session.user        Current logged-in user object (no password)


================================================================================
6. KEY FEATURES IMPLEMENTED
================================================================================

REGISTRATION
  - First name, last name, student ID, email, college, account type, password
  - Validates: password match, min 8 chars, 8-digit student ID
  - Rejects duplicate email or student ID
  - Account type: Student or Lab Technician

LOGIN
  - Authenticates email + password against accounts store
  - Invalid credentials show error message
  - "Forgot password" shows password hint (demo only)
  - Separate technician login page (adminsignup.html)

RESERVATIONS
  - Select lab, seat, date, time slot (30-min intervals, 7:30 AM - 5:30 PM)
  - Seat map with available/occupied/reserved/selected states
  - Anonymous reservation option
  - Booking summary panel
  - Cancel, edit, and rebook actions
  - All reservations persist in localStorage across page navigations
  - Technicians see all users' reservations with email labels

WALK-IN (Technician Only)
  - Enter student ID, select lab, seat, date, time
  - Validates student ID format and seat format
  - Looks up student name from accounts if registered
  - Saves to global reservation store with isWalkIn flag
  - Right panel shows current walk-in reservations from storage
  - Remove no-show button cancels the reservation in storage

DASHBOARD
  - Dynamic stats computed from actual reservation data
  - Upcoming reservations list
  - Quick actions (Reserve, Manage, Walk-In for techs, Profile, Find Users)
  - Recent activity feed
  - Favorite labs section
  - Technician dashboard shows all-user data

PROFILE
  - View and edit personal information
  - Statistics computed from reservation data
  - Change password (validates current password)
  - Change avatar (file upload, max 2MB)
  - Email notification toggle
  - Delete account (removes from all stores)
  - Role badge shows Student or Technician

SEARCH
  - Global search bar in navbar
  - Lab search on available slots page
  - User search by name, ID, or college filter


================================================================================
7. HANDLEBARS TEMPLATES (src/views/)
================================================================================

All pages are rendered server-side using Express-Handlebars.
Each .hbs template is a blueprint for the front-end response, with
dynamic elements replaced/generated during server-side rendering.

Templates use two layouts:
  - auth.hbs:      For public/login pages (minimal chrome)
  - dashboard.hbs: For authenticated pages (navbar, sidebar)

Partials (reusable components):
  - head.hbs:    Shared <head> with CSS/JS includes
  - navbar.hbs:  Dynamic navbar (role-aware links)


================================================================================
8. HOW TO TEST
================================================================================

1. Ensure MongoDB is running (local or Atlas)
2. Create a .env file with MONGODB_URI, SESSION_SECRET, PORT
3. Run: npm install
4. Run: node seed.js          (seeds sample data into MongoDB)
5. Run: npm start             (or npm run dev for nodemon)
6. Open http://localhost:3000 in a browser
7. Login with one of the default accounts above
8. To test as student: use maria_santos@dlsu.edu.ph / password123
9. To test as technician: use tech_admin@dlsu.edu.ph / admin123
10. To test registration: click "Sign up", fill the form, submit
11. To re-seed data: run node seed.js again (clears and re-inserts)


================================================================================
9. SAMPLE DATA
================================================================================

5 Computer Labs:
  AG1010  - Andrew Building      (30 seats)
  LS313   - La Salle Hall        (25 seats)
  GK101A  - Gokongwei Building   (40 seats)
  GK101B  - Gokongwei Building   (40 seats)
  GK304   - Gokongwei Building   (20 seats)

7 Default Reservations (seeded per user on first login):
  Mix of upcoming, completed, and cancelled statuses

8 Colleges:
  CCS, CLA, COB, COE, COS, GCOE, SOE, BAGCED

20 Time Slots:
  30-minute intervals from 7:30 AM to 5:30 PM


================================================================================
10. BACKEND DISTRIBUTION & CONTRIBUTIONS
================================================================================

-- Person 1 — Benedict
   JSON and Fetch | Handlebars | Cookies and Sessions

-- Person 2 — Ivan
   Node.js | Express | REST

-- Person 3 — Chrisander
   MongoDB | Mongoose | Password Hashing


================================================================================
11. BENEDICT'S BACKEND CONTRIBUTIONS
================================================================================

A. JSON AND FETCH
-----------------
Replaced all localStorage reads/writes in the frontend with fetch() calls
to the backend REST API. The frontend no longer stores or reads data from
localStorage directly — all data flows through HTTP requests to the
Node.js server.

Key changes to js/app.js:
  - authenticateUser() now sends POST /api/login with JSON body
    {email, password} and receives a session cookie on success.
  - registerAccount() now sends POST /api/register with JSON body
    containing all registration fields.
  - getReservations() now sends GET /api/reservations and parses
    the JSON response to populate the reservations page.
  - saveReservation() now sends POST /api/reservations with the
    reservation object as JSON.
  - cancelReservation() sends DELETE /api/reservations/:id.
  - editReservation() sends PUT /api/reservations/:id with updated fields.
  - getAllReservations() (technician) sends GET /api/reservations/all.
  - updateProfile() sends PUT /api/profile with updated user fields.
  - changePassword() sends PUT /api/profile/password with
    {currentPassword, newPassword}.
  - deleteAccount() sends DELETE /api/profile.
  - Walk-in endpoints: POST /api/walkin, GET /api/walkin,
    DELETE /api/walkin/:id.
  - All fetch calls include { credentials: 'include' } to send session
    cookies with every request.
  - Error responses from the server (4xx/5xx) are caught and displayed
    to the user via the existing alert/toast UI.

B. HANDLEBARS
-------------
Converted all page views from jQuery DOM manipulation to server-side
Handlebars templates. Each page builder function in app.js was replaced
by a corresponding .hbs file rendered by the Express server.

Template files created (src/views/):
  src/views/
    layouts/
      auth.hbs              Layout for public pages
      dashboard.hbs         Layout for authenticated pages
    partials/
      head.hbs              Shared <head> with CSS/JS includes
      navbar.hbs            Dynamic navbar (role-aware links)
    index.hbs               Landing page
    login.hbs               Login form
    register.hbs            Registration form
    adminsignup.hbs         Technician login
    dashboard.hbs           User dashboard with stats and quick actions
    cmpslots.hbs            Lab availability and seat maps
    reserve.hbs             Seat selection and reservation form
    reservations.hbs        View/manage reservations list
    profile.hbs             User profile with edit mode
    public-profile.hbs      Other user's public profile
    users.hbs               User search and directory
    walkin.hbs              Walk-in reservation (technician only)
    changepassword.hbs      Change password form
    error.hbs               Generic error page (404, 403, 500)

Handlebars helpers registered:
  - {{eq a b}}              Equality check for conditionals
  - {{formatDate date}}     Formats ISO date to readable string
  - {{formatTime slot}}     Maps time value to display label
  - {{getInitials name}}    Returns first-letter initials for avatars
  - {{ifTechnician role}}   Block helper for technician-only content
  - {{json obj}}            Serializes object for inline <script> data

C. COOKIES AND SESSIONS
------------------------
Implemented server-side session management using express-session with
a MongoDB session store (connect-mongo).

Setup (in index.js — the main entry point):
  - express-session configured with:
      secret: environment variable SESSION_SECRET
      resave: false
      saveUninitialized: false
      cookie: { maxAge: 1000*60*60*24*7 } (7-day sessions)
      store: MongoStore.create({ mongoUrl: MONGO_URI })
  - On successful login, the user object (without password) is stored
    in req.session.user.
  - authMiddleware checks req.session.user on protected routes and
    redirects to /login if absent.
  - techMiddleware additionally checks req.session.user.accountType
    === 'technician' for technician-only routes.
  - Logout destroys the session via req.session.destroy() and clears
    the cookie.
  - "Remember Me" checkbox sets a longer maxAge (21 days) on the
    session cookie.

Session-protected routes:
  GET  /dashboard          → authMiddleware
  GET  /reservations       → authMiddleware
  GET  /reserve            → authMiddleware
  GET  /profile            → authMiddleware
  PUT  /api/profile        → authMiddleware
  POST /api/reservations   → authMiddleware
  GET  /walkin             → authMiddleware + techMiddleware
  POST /api/walkin         → authMiddleware + techMiddleware


================================================================================
12. BACKEND TODO (Remaining Work for Ivan & Chrisander)
================================================================================

IVAN — Node.js, Express, REST API
----------------------------------
[TODO] Implement index.js (main entry point)
       - Set up Express app on port 3000
       - Import DB connection from ./src/models/conn.js
       - Import main router from ./src/routes/mainRouter.js
       - Configure express-handlebars (views in src/views/)
       - Configure express-session with connect-mongo store
       - Mount static file middleware for public/
       - Connect to MongoDB via conn.js
       - Mount the single main router

[TODO] Implement src/routes/mainRouter.js (single router)
       All page + API routes in one file (following mc3-debttracker
       convention of "just one" router for the meantime):
         GET /                      → render index.hbs
         GET /login                 → render login.hbs
         GET /register              → render register.hbs
         GET /adminsignup           → render adminsignup.hbs
         GET /dashboard             → authMiddleware → render dashboard.hbs
         GET /cmpslots              → authMiddleware → render cmpslots.hbs
         GET /reserve               → authMiddleware → render reserve.hbs
         GET /reservations          → authMiddleware → render reservations.hbs
         GET /profile               → authMiddleware → render profile.hbs
         GET /profile/:id           → authMiddleware → render public-profile.hbs
         GET /users                 → authMiddleware → render users.hbs
         GET /walkin                → techMiddleware → render walkin.hbs
         GET /changepassword        → authMiddleware → render changepassword.hbs
         POST /api/login            → authController.login
         POST /api/register         → authController.register
         POST /api/logout           → authController.logout
         GET  /api/reservations     → reservationController.getAll
         POST /api/reservations     → reservationController.create
         PUT  /api/reservations/:id → reservationController.update
         DELETE /api/reservations/:id → reservationController.cancel
         GET  /api/labs             → labController.getAll
         GET  /api/labs/:code/seats → labController.getSeats
         GET  /api/users            → userController.search
         PUT  /api/profile          → userController.updateProfile
         PUT  /api/profile/password → userController.changePassword
         DELETE /api/profile        → userController.deleteAccount
         POST /api/walkin           → walkinController.create
         GET  /api/walkin           → walkinController.getAll
         PUT  /api/walkin/:id/remove → walkinController.removeNoShow

[TODO] Implement src/middleware/auth.js
       - authMiddleware — check req.session.user
       - techMiddleware — check accountType === 'technician'

[TODO] Implement controller logic (src/controllers/)
       - Each controller imports models from ../models/
       - Controllers query the database via Mongoose models,
         then either render a Handlebars view or return JSON.
       - POST/PUT/DELETE routes return JSON { success, message, data }.
       - GET page routes pass the session user + queried data to the
         Handlebars template.

[TODO] Add form validation on the server side
       - Validate all POST/PUT request bodies (required fields, formats).
       - Return 400 with error messages for invalid input.
       - Student ID must be 8 digits.
       - Email must end with @dlsu.edu.ph.
       - Password minimum 8 characters.

[TODO] Add error handling
       - 404 handler for unknown routes → render error.hbs
       - Global error middleware → render error.hbs with 500


CHRISANDER — MongoDB, Mongoose, Password Hashing
--------------------------------------------------
[TODO] Set up MongoDB (local or Atlas)
       - Create database: openlab_db
       - Configure connection string in .env file
       - MONGO_URI=mongodb://localhost:27017/openlab_db (or Atlas URL)

[TODO] Create Mongoose models (src/models/)
       src/models/
         User.js
           - firstName:    String, required
           - lastName:     String, required
           - email:        String, required, unique, lowercase
           - password:     String, required (stored hashed)
           - studentId:    String, required, unique
           - college:      String, required, enum of 8 colleges
           - accountType:  String, enum ['student', 'technician']
           - avatar:       String (base64 or file path)
           - bio:          String
           - notifications: Boolean, default true
           - createdAt:    Date, default Date.now

         Reservation.js
           - user:         ObjectId, ref 'User', required
           - lab:          String, required (lab code e.g. "AG1010")
           - seat:         String, required (e.g. "A3")
           - date:         Date, required
           - timeSlot:     String, required (e.g. "07:30")
           - status:       String, enum ['confirmed','completed','cancelled']
           - isAnonymous:  Boolean, default false
           - isWalkIn:     Boolean, default false
           - createdAt:    Date, default Date.now

         Lab.js
           - code:         String, required, unique
           - building:     String, required
           - totalSeats:   Number, required
           - rows:         [String]
           - cols:         Number
           - hours:        String

[DONE] Create seed script (seed.js)
       - Connects to MongoDB, seeds 5 labs, 6 users, 7 reservations
       - Imports models from ./src/models/
       - Run with: node seed.js

[TODO] Implement password hashing
       - Use bcrypt (bcryptjs) for hashing
       - Hash password on registration: bcrypt.hash(password, 10)
       - Compare on login: bcrypt.compare(inputPassword, storedHash)
       - Hash new password on password change
       - Pre-save hook on User model to auto-hash if password is modified

[TODO] Ensure all existing passwords in the database are hashed
       - The seed script must hash all sample user passwords
       - No plaintext passwords should exist in MongoDB


================================================================================
13. FULL FILE STRUCTURE (mc3-debttracker convention)
================================================================================

project-root/
  index.js                   Express app entry point (port 3000)
  package.json               Dependencies and scripts
  .env                       MONGODB_URI, SESSION_SECRET, PORT
  seed.js                    Database seed script

  public/                    Static folder (express.static)
    css/
      style.css              All styling
    js/
      app.js                 Frontend JS (fetch-based)
    assets/
      bg.png                 Background image

  src/
    models/                  Mongoose schemas ("database/models")
      conn.js                MongoDB connection module
      User.js                User schema (bcrypt hooks)
      Reservation.js         Reservation schema (compound index)
      Lab.js                 Lab schema
    routes/
      mainRouter.js          Single router — all page + API routes
    controllers/
      authController.js      Login, register, logout
      labController.js       Lab data and seat availability
      reservationController.js  Reservation CRUD
      userController.js      User search, profile, password
      walkinController.js    Walk-in reservation (technician)
    middleware/
      auth.js                authMiddleware + techMiddleware
    helpers/
      hbs-helpers.js         Handlebars helpers
    views/
      layouts/
        auth.hbs             Layout for public pages
        dashboard.hbs        Layout for authenticated pages
      partials/
        head.hbs             HTML <head> partial
        navbar.hbs           Navigation bar
      index.hbs
      login.hbs
      register.hbs
      adminsignup.hbs
      dashboard.hbs
      cmpslots.hbs
      reserve.hbs
      reservations.hbs
      profile.hbs
      public-profile.hbs
      users.hbs
      walkin.hbs
      changepassword.hbs
      error.hbs

  specs/                     Project specification PDFs


================================================================================
14. BACKEND IMPLEMENTATION ROADMAP
================================================================================

Instructions: Delete each item once it is fully implemented and tested.
Progress is tracked by what remains in this list.

------------------------------------------------------------------------
PHASE A — Foundation (Do these first, everything else depends on them)
------------------------------------------------------------------------

[ ] A1. Implement index.js (main entry point)
      - Import DB connection from ./src/models/conn.js
      - Import main router from ./src/routes/mainRouter.js
      - Import helpers from ./src/helpers/hbs-helpers
      - Configure Handlebars (views in src/views/, layouts, partials)
      - Set up express.json(), express.urlencoded(), express.static('public')
      - Configure express-session with MongoStore
      - Mount the single main router
      - Add 404 handler and global error middleware
      - Start server and connect to DB via conn.js

[ ] A2. Implement User model (src/models/User.js)
      - Define schema: firstName, lastName, email (unique, lowercase),
        password, studentId (unique), college (enum), accountType (enum),
        bio, avatarUrl, avatarClass, notifications, timestamps
      - Add pre-save hook: hash password with bcrypt if modified
      - Add instance method: comparePassword(candidatePassword)
      - Create indexes on email and studentId

[ ] A3. Implement Lab model (src/models/Lab.js)
      - Define schema: code (unique), building, buildingKey, totalSeats,
        rows, cols, hours
      - No special hooks needed

[ ] A4. Implement Reservation model (src/models/Reservation.js)
      - Define schema: user (ObjectId ref User), lab, seat, building,
        date, timeSlot, status (enum: upcoming/completed/cancelled),
        anonymous, isWalkIn, createdBy (ObjectId ref User), timestamps
      - Add compound index on { lab, seat, date, timeSlot, status }
        to prevent double-booking

[ ] A5. Implement auth middleware (src/middleware/auth.js)
      - authMiddleware: check req.session.user exists, else redirect
        to /login (for page routes) or return 401 JSON (for API routes)
      - techMiddleware: check req.session.user.accountType === 'technician',
        else render error.hbs with 403 or return 403 JSON

[ ] A6. Implement seed.js
      - Connect to MongoDB
      - Clear existing User, Reservation, Lab collections
      - Insert 5 labs (AG1010, LS313, GK101A, GK101B, GK304)
      - Insert 6 users with bcrypt-hashed passwords
        (maria_santos, jose_rizal, ana_lopez, karl_mendoza,
         lea_cruz, tech_admin)
      - Insert sample reservations (mix of upcoming, completed, cancelled)
      - Disconnect and log results

------------------------------------------------------------------------
PHASE B — Authentication Routes (Login/Register/Logout)
------------------------------------------------------------------------

[ ] B1. Implement src/controllers/authController.js
      - login: validate email+password, bcrypt.compare, set req.session.user
      - register: validate fields, check duplicates, create User, set session
      - logout: req.session.destroy(), clear cookie

[ ] B2. Add auth routes to src/routes/mainRouter.js
      - POST /api/login        → authController.login
      - POST /api/register     → authController.register
      - POST /api/logout       → authController.logout

------------------------------------------------------------------------
PHASE C — Page Routes (Render Handlebars views)
------------------------------------------------------------------------

[ ] C1. Add page routes to src/routes/mainRouter.js
      - GET /                  → render index.hbs
      - GET /login             → render login.hbs (redirect if logged in)
      - GET /register          → render register.hbs (redirect if logged in)
      - GET /adminsignup       → render adminsignup.hbs
      - GET /dashboard         → authMiddleware → render dashboard.hbs
        with user stats, upcoming reservations, recent activity
      - GET /cmpslots          → render cmpslots.hbs with lab data
      - GET /reserve           → authMiddleware → render reserve.hbs
      - GET /reservations      → authMiddleware → render reservations.hbs
      - GET /profile           → authMiddleware → render profile.hbs
      - GET /public-profile/:id → render public-profile.hbs
      - GET /users             → render users.hbs
      - GET /walkin            → authMiddleware + techMiddleware → walkin.hbs
      - GET /changepassword    → authMiddleware → changepassword.hbs

------------------------------------------------------------------------
PHASE D — Core API Routes (Reservations, Labs, Users)
------------------------------------------------------------------------

[ ] D1. Implement src/controllers/reservationController.js
      - getUserReservations: find by user ID, sort by date desc
      - getAllReservations: (technician) find all, populate user
      - createReservation: validate no conflict, create document
      - updateReservation: find by ID, verify ownership, update fields
      - cancelReservation: find by ID, verify ownership, set status cancelled

[ ] D2. Add reservation API routes to src/routes/mainRouter.js
      - GET    /api/reservations      → authMiddleware → getUserReservations
      - GET    /api/reservations/all  → authMiddleware + techMiddleware
                                        → getAllReservations
      - POST   /api/reservations      → authMiddleware → createReservation
      - PUT    /api/reservations/:id  → authMiddleware → updateReservation
      - DELETE /api/reservations/:id  → authMiddleware → cancelReservation

[ ] D3. Implement src/controllers/labController.js
      - getAllLabs: return all labs
      - getLabAvailability: for a given lab+date, return seat statuses
        by cross-referencing reservations
      - getLabSeats: return seat grid for a specific lab

[ ] D4. Add lab API routes to src/routes/mainRouter.js
      - GET /api/labs                 → getAllLabs
      - GET /api/labs/:code/seats     → getLabSeats
      - GET /api/labs/:code/availability → getLabAvailability

[ ] D5. Implement src/controllers/userController.js
      - searchUsers: filter by name, studentId, college; paginate
      - getPublicProfile: find user by ID, exclude password
      - updateProfile: validate + update current user fields
      - changePassword: verify old password, hash new, save
      - deleteAccount: remove user + their reservations

[ ] D6. Add user API routes to src/routes/mainRouter.js
      - GET    /api/users             → searchUsers
      - GET    /api/users/:id         → getPublicProfile
      - PUT    /api/profile           → authMiddleware → updateProfile
      - PUT    /api/profile/password  → authMiddleware → changePassword
      - DELETE /api/profile           → authMiddleware → deleteAccount

------------------------------------------------------------------------
PHASE E — Walk-In Routes (Technician only)
------------------------------------------------------------------------

[ ] E1. Implement src/controllers/walkinController.js
      - createWalkIn: validate student ID, create reservation with
        isWalkIn=true, createdBy=technician user ID
      - getWalkIns: find reservations where isWalkIn=true for today
      - removeNoShow: find walk-in by ID, set status cancelled

[ ] E2. Add walk-in API routes to src/routes/mainRouter.js
      - POST   /api/walkin            → authMiddleware + techMiddleware
                                        → createWalkIn
      - GET    /api/walkin            → authMiddleware + techMiddleware
                                        → getWalkIns
      - DELETE /api/walkin/:id        → authMiddleware + techMiddleware
                                        → removeNoShow

------------------------------------------------------------------------
PHASE F — Polish & Integration
------------------------------------------------------------------------

[ ] F1. Server-side form validation
      - Validate all POST/PUT bodies (required fields, formats)
      - Email must end with @dlsu.edu.ph
      - Student ID must be 8 digits
      - Password minimum 8 characters
      - Return 400 with descriptive error messages

[ ] F2. Error handling
      - 404 catch-all → render error.hbs with "Page not found"
      - Global error middleware → render error.hbs with 500
      - Consistent JSON error format for API routes

[ ] F3. Frontend fetch integration testing
      - Verify all fetch() calls in public/js/app.js hit the correct
        endpoints defined in src/routes/mainRouter.js
      - Test CRUD flows: register → login → reserve → cancel → logout
      - Test technician flows: walk-in → view all → remove no-show

[ ] F4. Session edge cases
      - Expired session redirects to login gracefully
      - "Remember Me" extends cookie maxAge to 21 days
      - Session persists across server restarts (MongoStore)

[DONE] F5. Folder reorganization (mc3-debttracker convention)
      - Removed legacy pages/ directory (old HTML files)
      - Removed index.html (replaced by src/views/index.hbs)
      - Removed duplicate assets/ and js/ directories
      - Renamed server.js → index.js (entry point at root)
      - Moved all source into src/ (models, routes, controllers,
        middleware, helpers, views)
      - Created src/models/conn.js for DB connection module
      - Consolidated all routes into src/routes/mainRouter.js
      - All static assets served from public/
      - Confirm .env is in .gitignore

================================================================================
END OF ROADMAP — Delete items above as they are completed.
================================================================================
